CREATE TABLE user (
                  `id`       VARCHAR(255) NOT NULL AUTO_INCREMENT ,
                  `name`     VARCHAR(255) NOT NULL ,
                  `password` INT          NOT NULL ,
                  PRIMARY KEY ( `id` )
);